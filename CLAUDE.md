# Gleam

## Architecture Scale

Scale: minimal-mvvm
Confirmed by user. Menubar utility, few screens, no data pipeline —
protocols only at system boundaries, native components, inline views
until reused, tokens only for shared values.
